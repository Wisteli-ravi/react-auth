export enum SuperAdminRoutesRef {
    Dashboard = "/",
    User = "/user",
    Login = "/login",
    Client = "/client",
}

export enum ClientAdminRoutesRef {
    Dashboard = "dashboard",
    User = "/user",
    Login = "/login",
    Client = "/client",
}

export enum AdminRoutesRef {
    User = "/user",
    Login = "/login",
    Client = "/client",
}
  
  export enum CommonRoutesRef {
    Login = "/login",
    ForgotPassword = "forgotPassword",
    MyProfile = "/my_profile",
    Info = "/info"
  }